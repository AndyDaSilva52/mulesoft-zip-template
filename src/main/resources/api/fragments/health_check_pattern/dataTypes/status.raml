#%RAML 1.0 DataType

type: object
properties:
  status:
    type: string
    description: The status of the current API or a external service (API, Database, Message Broker, etc.).
    enum: [ "UP", "DOWN", "UNKNOWN" ]